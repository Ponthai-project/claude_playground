# 次セッションの冒頭で投げるプロンプト

以下をそのままコピーして投げる。

---

```
C:\Users\topge\OneDrive\ドキュメント\GitHub\claude_playground\work\hook-utf8-fix\引継ぎ_パーミッション是正_2026-08-25.md を読んで、続きをやって。

作業場所は claude_playground 配下。.claude 配下は読むだけにして、書き込み（フックの差し替え・settings.json の変更）は必ず承認を取ってから。

残件1（修正版フックの検分とBOM合成）から順に。前回のように推論で「直ったはず」と判断せず、実機で確かめてから報告すること。
```

---

## 補足（プロンプトに含めなかった背景）

上記プロンプトは短くしてあるが、引継ぎ書の0節・5節・7節を読めば以下は自動的に伝わる。

- **現在も本番は破れている**（`~/.claude/hooks/block-dangerous.ps1` は危険コマンドを一切検知できない状態）
- 承認済みの方針は2点：playground で検証してから承認を得て適用すること、allow 札 `Bash(git -C *)` は「安全なサブコマンドに限定」する方向
- 残件は9項目あり、優先順位付きで7節に記載

## 途中から再開する場合のプロンプト例

残件1〜3が済んでいる状態で再開するなら、末尾を差し替える。

```
残件4（フックの検知漏れ：--no-verify の長綴りと、branch -D / filter-branch / rm / mv / checkout -f / update-ref / config）から。
```

適用段階まで進んでいるなら：

```
残件5（.claude への適用）の承認を取るところから。適用後は残件6の実地確認（本番で git -C . reset --hard HEAD が Permission denied で止まること）まで必ずやり切ること。
```
