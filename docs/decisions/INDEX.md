# 意思決定記録 索引（claude_playground）

`claude_playground` 配下の作業、および本リポジトリで進めている
skill `graphreco-html`（実体は `~/.claude/skills/graphreco-html/`）の改善に関する
意思決定記録の索引。運用の詳細は `~/.claude/docs/worklog-rules.md` および skill `worklog` を参照。

> グラレコ関連の記録は、殿のご指示により2026-08-25にこちらへ一本化した。
> 以前は `~/.claude/docs/decisions/` に置いていたが、同索引は「`~/.claude` 配下の記録専用」と
> 建て付けられているため、シリーズごとこちらへ移設している。

| 日付 | type | タイトル | 決定 | ファイル |
|---|---|---|---|---|
| 2026-08-29 | ops | work配下の試行スクリーンショットを履歴に入れない | 未追跡だった `work/` 一式のコミットに際し、16MBに及ぶ検証用の使い捨てPNG（`crop_*.png`/`render_*.png` 等）のみ除外し `work/**/*.png` を `.gitignore` へ追加。「全部そのまま」（履歴が恒久的に肥大し、抜くには履歴書き換えが要る）と「`work/` 全体を見送る」（故障注入ケース一式と引継ぎ文書まで失う）を却下し、再現手順の残る資産＝HTML/MD/スクリプトのみ残す方針を採った | — （索引のみ） |
| 2026-08-25 | ops | フックのUTF-8経路是正とexit伝播の是正 | フックの終了処理を全て `[System.Environment]::Exit(N)` に統一（本番の `&` 呼び出し形では `exit 2` が 1 に化けることを実測）。fail-safe は ask のまま据え置き、allow 札の絞り込みとの同時適用を必須条件とする | [2026-08-25-ops-フックのUTF8経路是正とexit伝播の是正.md](2026-08-25-ops-フックのUTF8経路是正とexit伝播の是正.md) |
| 2026-08-25 | doc | 揺らぎフィルタの座標系取り違えの是正 | 揺らぎが小に効かず大に効きすぎる根因は座標系の取り違え。大きさの帯ごとに `hand-wobble-xs`/`-s`/`-m`/`-l` の4本へ分離し、帯に無い大きさは `baseFrequency≒4.5/V` と「絵は比例・枠は絶対量固定」の二式から起こす | [2026-08-25-doc-揺らぎフィルタの座標系取り違えの是正.md](2026-08-25-doc-揺らぎフィルタの座標系取り違えの是正.md) |
| 2026-08-23 | doc | グラレコ手描き感の改善方針 | MCPは主因に効かぬため不採用、揺らぎフィルタ既定適用・線幅の不揃い化・丸書体是正・検品必須化の第1陣で規約を執行させる | [2026-08-23-doc-グラレコ手描き感の改善方針.md](2026-08-23-doc-グラレコ手描き感の改善方針.md) |
